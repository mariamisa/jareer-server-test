# auth server with login / sign up

### setup project
* pgcli setup 
* install packages :  ``` npm i ```
* .env file
    ```
    DEV_DB_URL= postgres://mariam:123@localhost:5432/authtest
    SECRET_KEY=secret
     ```